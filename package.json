{
  "name": "codemiles",
  "version": "1.0.0",
  "description": "Mobile-first AI-powered Git assistant",
  "scripts": {
    "postinstall": "cd frontend && npm install && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start"
  }
}
